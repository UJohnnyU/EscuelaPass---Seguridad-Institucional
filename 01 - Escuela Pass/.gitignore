**/__pycache__/

node_modules
dist
.env
.env.local
.env.e2e
coverage
*.log
uploads/comprobantes/*
!uploads/comprobantes/.gitkeep
.vercel
.env*.local

# Claves locales (Firebase Admin, etc.) — no subir al repositorio
.secrets/

# Generado desde frontend/.env por scripts/sync-fcm-sw.mjs (no versionar con claves del proyecto).
frontend/public/firebase-messaging-sw.js

# Design assets / local images (not versioned)
Img/
